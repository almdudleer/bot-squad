---
assignment: operator
kind: operator
sid: S-almdudleer-operator-p533
updated: 2026-07-30T17:00:04Z
---

# Operator state — bot-squad (p533 → successor, 2026-07-31 00:35Z)

**HOW TO READ THIS: forward state, not a log.** Prune process + section routing:
`docs/runbook/D-0068`. The sanctioned `##` sections are a CLOSED list of **five**
— Priorities · What's happening now · Delivered · Next · Tracked issues — and the
SSOT is **code**, `OPERATOR_STATE_SECTIONS` at
`worker/bot_squad_worker/assignment.py:318`, not this header. **Audit on arrival:
`grep -n '^## '` AND the line count.** Bound is D-0068 §1.3: *would a successor act
on this line next session?* **Evidence goes on the TICKET; this file keeps the
forward half and a pointer.** Lessons route to memory — do not re-derive them here.

⚠ **THE AUDIT DOES NOT CURRENTLY PASS, AND THAT IS KNOWN — filed as T-0837.**
This file has **SEVEN** `##` sections. Two are **off-schema**: this one
(`⚠⚠ READ BEFORE TOUCHING ANYTHING`) and `## Standing directives (stakeholder)`
— and D-0068 §1 names `STANDING DIRECTIVES` explicitly as an unfiled-knowledge
alarm. **Do not read the mismatch as your own error or as a section someone added
wrongly; it is an unfinished prune, inherited.** Its content is load-bearing, so
it is being ROUTED to real homes rather than deleted (D-0068 §4b is the loss case
that happens when it is cut in a hurry). ★ **p533 wrote "the five sections are a
CLOSED list" into this header while the file had seven — the audit instruction was
correct and its own author's claim about the file was not.** Purest local specimen
of the day's theme: **a precise, confident statement with no measurement behind
it.** The instrument worked; run it, and believe it over the prose next to it.

**YOUR JOB:** human-facing dispatcher for ONE project. Triage his intent, spawn
TLs/devs, curate the board, decide what ships. You do NOT write feature code and you
do NOT run deploys — a TL executes; you decide *what* and *when*.

---


## ⚠⚠ READ BEFORE TOUCHING ANYTHING

<body elided — see the full 55832-byte specimen in the ticket evidence dir>

## Priorities

<body elided — see the full 55832-byte specimen in the ticket evidence dir>

## What's happening now

<body elided — see the full 55832-byte specimen in the ticket evidence dir>

## Delivered

<body elided — see the full 55832-byte specimen in the ticket evidence dir>

## Next

<body elided — see the full 55832-byte specimen in the ticket evidence dir>

## Tracked issues

<body elided — see the full 55832-byte specimen in the ticket evidence dir>

## Standing directives (stakeholder)

<body elided — see the full 55832-byte specimen in the ticket evidence dir>

